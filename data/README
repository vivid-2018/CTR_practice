此文件包含原始movielen数据（20M）tags.csv。
tags.csv里面包含四列，userId，movieId，tag以及timestamp。每一行表示某用户给某电影打上了某标签，tags.csv中的所有样例构成正例，负样本通过代码(data_generate.ipynb)生成，正负比例大约为1：3。生成之后的数据为data.csv。
data.csv为所有模型的数据，其中所有字段均未经过LabelEncoder处理，时间戳信息未使用。详情请移步data_generate.ipynb
